import AWS from "aws-sdk";

import React from 'react';

const ConfigRekognition = () => {
    return (
        <div>
            <input type='file'/>
        </div>
    );
};

export default ConfigRekognition;